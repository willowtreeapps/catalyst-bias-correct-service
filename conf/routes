# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /corrector              controllers.HomeController.index
POST    /corrector/correct      controllers.CorrectionController.correct(request: Request)
